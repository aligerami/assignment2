file_address=""
file_address=input("Enter the name of directory: ")

def number_of_files(address):
    return 
    os.chdir('/home/user/workspace')
    all_subdirs = [d for d in os.listdir('.') if os.path.isdir(d)]
    for dirs in all_subdirs:
    number_of_files(dir)
    current = os.getcwd()
    new = str(current).split("/")[4]
    print new